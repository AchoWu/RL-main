# Old-baseline reverse OPD with equal total weight for every valid sequence.
# No TVD gate is configured: compared with distillation_math.yaml, this changes
# only the token_mean -> sequence_mean reduction.
defaults: distillation_math.yaml

loss_fn:
    zero_outside_topk: false
    reduction: "sequence_mean"
